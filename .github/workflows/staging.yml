name: Deploy Vite React App - MTN Staging Dashboard

on:
  push:
    branches:
      - staging

jobs:
  build-and-deploy:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Add SSH Known Hosts
        run: ssh-keyscan -H ${{ secrets.VM_IP }} >> ~/.ssh/known_hosts

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install
        env:
          CI: true

      - name: Build
        run: bun run build
        env:
          NEXT_PUBLIC_BASE_URL: ${{ secrets.NEXT_PUBLIC_BASE_URL }}

      - name: Deploy to VM
        run: |
          rsync -avz --delete ./dist/ ${{ secrets.VM_USER }}@${{ secrets.VM_IP }}:/var/www/html/MTN-FAQs_staging
